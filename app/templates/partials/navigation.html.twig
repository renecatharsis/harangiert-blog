<nav id="navbar"
     class="fixed top-0 left-0 w-full z-50 backdrop-blur-md text-white">
    <div class="max-w-6xl mx-auto px-6 py-4 flex items-center justify-between">
        <div class="text-xl font-extrabold">
            <a href="{{ sulu_content_root_path() }}" class="hover:text-blue-300">harangiert.de</a>
        </div>
        <button id="burger-menu" class="md:hidden flex flex-col justify-center items-center w-8 h-8 gap-1.5" aria-label="Menu" aria-expanded="false">
            <span class="w-6 h-0.5 bg-white transition-transform"></span>
            <span class="w-6 h-0.5 bg-white transition-opacity"></span>
            <span class="w-6 h-0.5 bg-white transition-transform"></span>
        </button>
        <div id="nav-links" class="hidden md:flex space-x-6 text-xs uppercase font-extrabold">
            {% for item in sulu_navigation_root_tree('main') %}
                <a href="{{ sulu_content_path(item.url) }}" class="hover:text-blue-300" title="{{ item.title }}">{{ item.title }}</a>
            {% endfor %}
        </div>
    </div>
    <div id="mobile-menu" class="hidden md:hidden absolute top-full left-0 w-full bg-zinc-800/95 backdrop-blur-md py-4 px-6 space-y-4">
        {% for item in sulu_navigation_root_tree('main') %}
            <a href="{{ sulu_content_path(item.url) }}" class="block hover:text-blue-300" title="{{ item.title }}">{{ item.title }}</a>
        {% endfor %}
    </div>
</nav>
